Code Libraries:
- Use latest version of jQuery from Google.
- Use reset.css.
- Use ChunkFive Webfont kit from Font Squirrel.

JS Behavior:
- "Beautiful Trees" should initially not be displayed. It should fade-in on hover.
- Callouts should initially not be expanded and should slide down on click.

Compatibility:
- Site must render properly and behave (JS) on latest versions of Chrome, Firefox, Safari, Mobile Safari, IE9+.
- Site must utilize media queries for screens 480px and smaller, at which a single column page and only one callout should be displayed. No JS interactions should apply below 480px.

Misc:
- 960px wide hero image is supplied.